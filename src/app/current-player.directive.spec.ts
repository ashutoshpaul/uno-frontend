import { CurrentPlayerDirective } from './current-player.directive';

describe('CurrentPlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentPlayerDirective();
    expect(directive).toBeTruthy();
  });
});
