export enum CARD_ANIMATION_ENUM {
  stationary = "stationary",
  prompt = "prompt",
  peep = "peep",
  discard = "discard",
}

export enum OPPONENT_CARD_ANIMATION_ENUM {
  stationary = "stationary",
  discard = "discard",
}