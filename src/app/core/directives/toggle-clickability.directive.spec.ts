import { ToggleClickabilityDirective } from './toggle-clickability.directive';

describe('ToggleClickabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleClickabilityDirective();
    expect(directive).toBeTruthy();
  });
});
