import { CardGlowDirective } from './card-glow.directive';

describe('CardGlowDirective', () => {
  it('should create an instance', () => {
    const directive = new CardGlowDirective();
    expect(directive).toBeTruthy();
  });
});
