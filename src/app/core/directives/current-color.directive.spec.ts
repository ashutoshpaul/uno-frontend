import { CurrentColorDirective } from './current-color.directive';

describe('CurrentColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrentColorDirective();
    expect(directive).toBeTruthy();
  });
});
